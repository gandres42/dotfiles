{
    "name": "WSNAME",
    "privileged": true,
    "remoteUser": "${env:USER}",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "USERNAME": "${env:USER}",
            "CONTAINER_ID": "${containerName}"
        }
    },
    "workspaceFolder": "/home/${env:USER}/WSNAME",
    "workspaceMount": "source=${localWorkspaceFolder},target=/home/${env:USER}/WSNAME,type=bind",
    "runArgs": [
        "--net=host",
        "--pid=host",
        "--ipc=host",
        "-e", "DISPLAY=${env:DISPLAY}"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "llvm-vs-code-extensions.vscode-clangd",
                "ms-vscode.cmake-tools"
            ]
        }
    },
    "mounts": [
       "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind,consistency=cached,optional=true",
       "source=/dev/dri,target=/dev/dri,type=bind,consistency=cached,optional=true",
       "type=bind,source=/home/${localEnv:USER}/.ssh,target=/home/${env:USER}/.ssh,readonly,optional=true",
       "type=bind,source=/home/${localEnv:USER}/.dotfiles,target=/home/${env:USER}/.dotfiles,readonly,optional=true",
       "type=bind,source=/home/${localEnv:USER}/Datasets,target=/home/${env:USER}/datasets,optional=true"
    ]
}